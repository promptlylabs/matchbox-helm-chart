# Matchbox Helm Chart Repository

```bash
helm repo add matchbox https://promptlylabs.github.io/matchbox-helm-chart
helm repo update
```

For chart documentation, see the [main branch](https://github.com/promptlylabs/matchbox-helm-chart).
